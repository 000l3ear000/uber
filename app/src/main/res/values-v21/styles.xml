<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="OverflowMenu" parent="@android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_menu_overflow</item>
        <item name="android:overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-4.0dip</item>
        <item name="android:popupBackground">@color/appThemeColor_1</item>
    </style>

    <style name="Theme_Transparent" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition" tools:targetApi="lollipop">
            @transition/custom_animation
        </item>
        <item name="android:windowExitTransition" tools:targetApi="lollipop">
            @transition/custom_animation
        </item>

        <!-- if we don't specify reenter transition, by default exit transition is taken -->
        <!-- if we don't specify return transition, by default enter transition is taken -->

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>

    </style>
</resources>
